# pytest looks for tests in the tests/ folder.
# pythonpath = . means imports like `from photo_planner.models import ...` work
# when you run pytest from weather-project/.

[pytest]
pythonpath = .
testpaths = tests
